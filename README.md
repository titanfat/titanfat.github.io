# Andrew Titov portfolio

Static Next.js portfolio for GitHub Pages.

```bash
npm install
npm run dev
```

Production build:

```bash
npm run build
```

The generated static site is written to `out/`. Push this repository to `titanfat/titanfat.github.io` and enable GitHub Pages with **GitHub Actions** as the source.
